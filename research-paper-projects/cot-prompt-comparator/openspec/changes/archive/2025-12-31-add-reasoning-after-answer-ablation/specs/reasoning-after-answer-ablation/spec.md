## ADDED Requirements

### Requirement: Reasoning-After-Answer Prompt
The project SHALL have a reasoning-after-answer prompt that takes a question and returns a formatted prompt.

#### Scenario:
- **WHEN** the `reasoningAfterAnswerPrompt` function is called with a question
- **THEN** it returns a string with the question formatted as a reasoning-after-answer prompt.
